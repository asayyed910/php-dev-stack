# php-dev-stack
A Valet-like local development environment for PHP applications powered by Docker. Supports Laravel, CodeIgniter 3, and Core PHP with automatic domain mapping (project.test), trusted HTTPS using mkcert, MailHog for email testing, and MySQL 5.7 with data persisted on the host machine. Projects live directly on macOS for maximum performance.
